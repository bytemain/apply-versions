# versions.toml - Version management configuration for apply-versions
# This project uses itself to manage versions (dogfooding!)

# Main npm package
[[package]]
type = "npm"
path = "."
name = "apply-versions"
version = "1.4.0"
# create_tag = true  # Optional: Create git tag when applying version (default: true)